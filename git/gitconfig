[user]
	name = Penghui Liao
	email = liaoishere@gmail.com
	signingkey = B847939A95E9B47A
[color]
	ui = true
[alias]
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
	co = checkout
	ci = commit -s
	br = branch
	st = status
	up = rebase
	last = log -1 HEAD
	unstage = reset HEAD --
	amend = commit --amend --reuse-message=HEAD
	n = !git --no-pager
[gitreview]
	username = LiaoPenghui
[push]
	default = simple
[core]
    excludesfile = /Users/Liao/.gitignore_global
	autocrlf = input
    pager = delta
	editor = /usr/local/bin/vim
[pager]
	branch = cat
[grep]
	extendedRegexp = true
[pull]
	ff = only
[url "git@github.xsky.com:"]
	insteadOf = git://github.xsky.com
[http "https://github.com"]
	proxy = socks5://127.0.0.1:7891

[delta]
    features = custom
    syntax-theme = Dracula
[interactive]
    diffFilter = delta --color-only

[delta "custom"]
    # author: https://github.com/clnoll
    commit-decoration-style = bold box ul "#34fd50"
    dark = true
    file-decoration-style = none
    file-style = omit
    hunk-header-decoration-style = "#6272a4" box ul
    hunk-header-file-style = "#999999"
    hunk-header-line-number-style = bold "#EED835"
    hunk-header-style = file line-number syntax
    line-numbers = true
    line-numbers-left-style = black
    line-numbers-minus-style = "#EE356A"
    line-numbers-plus-style = "#13CCE7"
    line-numbers-right-style = black
    line-numbers-zero-style = "#999999"
    minus-emph-style = normal "#BF0A3E"
    minus-style = normal "#844C5C"
    plus-emph-style = syntax "#198494"
    plus-style = syntax "#104850"
    side-by-side = true
    syntax-theme = Vibrant Sunburst
